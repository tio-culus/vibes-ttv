# Gemini トランスクライバーの5分ウィンドウ・3分オーバーラップ化およびプロンプト調整の実装計画

ユーザー様からの詳細なご指示に基づき、1ウィンドウあたりの発話密度が高い区間でも安全に完走できるよう、**5分ウィンドウ・3分オーバーラップ** へのチューニングを行います。また、読み上げBotの音声を無視せず、別の話者（Streamer）として識別・文字起こしするようにプロンプトを修正します。

---

## ユーザーレビューが必要な箇所

> [!IMPORTANT]
> - **ウィンドウ設計の調整（5分ウィンドウ、3分オーバーラップ）**:
>   * `window_size = 300.0` (5分)
>   * `overlap = 180.0` (3分)
>   * 各スライスの進み幅は2分（120秒）となり、マージ境界（Midpoint）は各オーバーラップの中間点（例：Window 1 と Window 2 の間は 210.0秒）に自動計算されます。
>   * これにより、ラグ等による話題の収束文脈を3分間のオーバーラップで確保しつつ、1ウィンドウあたりの出力トークン数を 8,192 トークンの制限から極めて安全なレベル（約3,000〜4,500トークン）に抑制します。
> - **読み上げBot（ボイスボット）の別話者扱い化**:
>   * `system_instruction` を変更し、ゆっくりボイスなどの読み上げBotの音声を「無視」するのではなく、「別のStreamer（例: [Streamer 1] や [Streamer 2] など）」として識別し、人間の発話と同様に話者分離を行って文字起こしを出力するように指示します。
> - **正規表現によるフォールバックパース（途切れ救出）の併装**:
>   * 5分ウィンドウ化によりトークン制限突破の危険性はほぼ無くなりますが、万が一JSONフォーマットが壊れた場合に備え、正規表現による救出ロジックを実装し、エラーでのクラッシュを防ぎます。

---

## 提案される変更箇所

### 1. Gemini トランスクライバーの改修

#### [MODIFY] [gemini_transcriber.py](file:///c:/Users/ratio/src/github.com/tio-culus/vibes-ttv/vibes_ttv/analyzers/stt/gemini_transcriber.py)
* **ウィンドウサイズとオーバーラップの設定**:
  * `window_size = 300.0`
  * `overlap = 180.0`
* **システムプロンプトの修正**:
  * 読み上げBot（ボイスボット）を無視せず、別の話者としてタグ（`[Streamer X]`）を付与して正確に書き起こすようプロンプトを改修します。
* **フォールバックパースの実装**:
  * `_transcribe_chunk` メソッドに、Pydanticのパース失敗時に `response.text` から正規表現で `{ "start": ..., "end": ..., "text": ... }` パターンを走査してパースを試みます。
  * 例外発生時には空リスト `[]` を返して全体の結合をバイパスさせます。

---

## 検証計画

### 1. 単体テストによるフォールバックパースの検証
* 意図的に破損させた JSON データをフォールバックパース関数に渡し、破損前のセグメントが正しく復旧できることを確認します。

### 2. Gemini ベンチマークの再実行
* `scratch/run_stt_benchmark.py --engine gemini` を実行し：
  * 音声が5分（3分オーバーラップ）ごとに切り出され、並列処理されることを確認します。
  * Window 7 (前回エラー区間) を含むすべてのウィンドウが正常に完走することを確認します。
  * 読み上げBotの音声が別の `[Streamer X]` タグ付きで文字起こしされていることを確認します。
  * 結合された最終結果の文字数比率が Whisper リファレンス比で大幅に改善（80%以上）することを確認します。
