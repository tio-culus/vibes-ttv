# 話題分類割合グラフ追加、VOD時間ジャンプリンク、UIチカチカ防止、および古いデータの安全な再分析ボタン実装計画書

ティオさんからご要望いただいた以下の4点の実装計画書です。特に「再分析失敗時の安全対策」を考慮した設計としています：
1. 「🗣️ 配信中の話題分析」タブ内、話題の盛り上がりランキングの直上に「話題分類の割合」を示すプレミアムなドーナツチャートを表示。
2. 話題タイムラインの配信時間および分類ラベル（ならびにランキングカード内の時間枠）に、Twitch VOD の開始時刻へ直接ジャンプするURLリンクを埋め込み。
3. バックグラウンド分析実行中の1秒ごとのポーリング（再描画）に伴う、前回の分析データ表示エリアの不快なチカチカ（明滅）現象の防止。
4. 古くて一部のデータ（コメント詳細等）が欠損している過去アーカイブの表示時に、その場で直接再分析を強制実行できる「🔄 再分析する」ボタンを追加。

## ユーザーレビューが必要な箇所
> [!IMPORTANT]
> - **安全なデータ差し替え（フェイルセーフ設計）**:
>   * 古くなったVODが Twitch から削除されて再取得できなくなるリスクに備え、**分析が100%完了するまで古いデータは削除しません。**
>   * 音声抽出・Whisper文字起こし・Geminiによる話題分析とコメント態度分析がすべて正常終了した「最後の保存フェーズ」でのみ、データベースのトランザクションを開始します。
>   * トランザクション内で、`DELETE`（旧トピック・旧統計データの削除）と `INSERT/MERGE`（新データ書き込み）を一括してアトミックにコミットします。
>   * 万が一、途中でダウンロードエラーやAPIエラーが発生しても、トランザクションは実行されないため、古い分析結果は一切無傷のままダッシュボードに残り続けます。
> - **サイドバーのUIリファクタリング**:
>   * 過去データの閲覧モード時でも、再分析ボタンから再分析を実行できるよう、Gemini APIキーやWhisperモデルなどの「分析パラメーター設定」をサイドバーに常に表示するレイアウトにリファクタリングします。これにより、閲覧モードのままAPIキーやバッチサイズを指定して、直ぐに再分析ボタンを押せるようにします。

---

## 提案される変更箇所

### 1. フロントエンドUIおよび実行トランザクションの追加

#### [MODIFY] [app.py](file:///c:/Users/ratio/src/github.com/tio-culus/vibes-ttv/vibes_ttv/app.py)
* `format_seconds` 関数の直下に、秒数を Twitch クエリパラメータ形式（例: `1h23m45s`）に変換する `format_twitch_offset` ヘルパー関数を新規追加します。
* 510行目の `<style>` タグ内に以下のCSSを追加して、毎秒の再描画による明滅（点滅）を解消します。
  ```css
  /* Prevent stale components from flickering during analysis runner reruns */
  [data-stale="true"] {
      opacity: 0.7 !important;
      transition: none !important;
  }
  ```
* サイドバーの表示ロジックを整理し、APIキー、Whisperモデル、バッチサイズ設定の3つのパラメーター項目を常に表示させます。
* 「👤 視聴者の態度分析」タブの「コメント分類詳細」エリアで、詳細データが欠損している（`comment_details_json` が空）場合の `st.info` 表示の直下に、「🔄 このアーカイブを再分析する」ボタンを追加します。
* ボタンが押されたら、現在表示中の `vod` のIDから Twitch VOD URLを再構成し、`AnalysisRunner` バックグラウンドスレッドを起動します。（ここではまだデータは消しません）
* `run_real_analysis` および `run_real_analysis_thread` 内の最後のDB保存箇所を、以下のようにトランザクション化し、データ生成が完了した段階でのみアトミックに旧データをクリア＆新データを適用します：
  ```python
  # db_stats および db_topics のデータが揃った段階でトランザクション実行
  db_session = db.get_session()
  try:
      # 旧関連データを一括削除
      db_session.query(Topic).filter_by(vod_id=vod_id).delete()
      db_session.query(VODListenerStats).filter_by(vod_id=vod_id).delete()
      
      # 新データの追加
      for t in db_topics:
          db_session.add(t)
      for s in db_stats:
          db_session.merge(s)
          
      # VOD処理時間の更新
      vod = db_session.query(VOD).filter_by(vod_id=vod_id).first()
      if vod:
          vod.chat_collection_time_seconds = t_chat_collection
          # ... (他のカラム更新) ...
      db_session.commit()
  except Exception as e:
      db_session.rollback()
      raise e
  finally:
      db.remove_session()
  ```

---

## 検証計画

### 1. 自動テストの実行
UIのロジック追加やヘルパー関数追加、トランザクションの挙動によって既存のテストケースが壊れていないかを検証します。
```bash
.\venv\Scripts\python -m pytest tests/test_vibes.py
```

### 2. 手動動作確認
1. 古い分析データ（もしくは、以前に分析した詳細データがないもの）をロードし、「👤 視聴者の態度分析」の「コメント分類詳細」セクションを確認します。
2. 「このアーカイブには個別のコメント分類詳細データが保存されていません」というメッセージと、その下に「🔄 このアーカイブを再分析する」ボタンが表示されていることを確認します。
3. サイドバーにAPIキーを設定した状態で「再分析する」ボタンを押し、バックグラウンドでの分析が起動して進捗率が表示されることを確認します。
4. **分析中にアプリを強制終了（またはわざと間違ったAPIキーで失敗）させ、以前の古い分析データが一切消えることなく無傷で残り続けていること**を検証します。
5. 正しいAPIキーで再分析が完了した後に、古いデータが新データ（分類詳細バッジ付き）に入れ替わることを確認します。
